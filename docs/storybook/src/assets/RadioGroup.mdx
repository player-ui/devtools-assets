import { Canvas, Meta } from '@storybook/blocks';
import * as RadioGroupStories from "./RadioGroup.stories";

<Meta of={ RadioGroupStories } />

# RadioGroup Asset

> type: `radio-group`

It provides a `RadioGroup` component that can present a way to input one of multiple choices.

## RadioGroup DSL Properties

| Property | Type | Required | Description |
| --- | --- | --- | --- |
| `binding` | `string` | false | The location in the data-model to store the data |
| `label` | `AssetWrapper<TextAsset> \| string` | false | A text asset for the radio group label |
| `values` | `Array<AssetWrapper<RadioItem>>` | false | A text asset for the radio group label |

## RadioItem DSL Properties

| Property | Type | Required | Description |
| --- | --- | --- | --- |
| `label` | `string` | true | The radio item label |
| `value` | `string` | true | The radio item value stored |

## Basic Use Case

<Canvas of={ RadioGroupStories.Basic } />